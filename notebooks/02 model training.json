{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# 02 Model Training\n",
    "\n",
    "**Purpose:** Train all models described in the paper: DeepTriage-CN (multimodal fusion), TabNet, Vitals-only XGBoost, Random Forest, Text-only Logistic Regression, and calculate clinical scores (NEWS2, MEWS, ESI).\n",
    "\n",
    "**Prerequisites:** Run `01_data_exploration.ipynb` first to load and split the data, or use the scripts in `src/`."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "import sys\n",
    "sys.path.append(\"..\")\n",
    "\n",
    "import numpy as np\n",
    "import pandas as pd\n",
    "import yaml\n",
    "import joblib\n",
    "import logging\n",
    "from pathlib import Path\n",
    "\n",
    "from src.preprocess import (\n",
    "    load_and_split_data,\n",
    "    preprocess_structured,\n",
    "    preprocess_text,\n",
    "    get_labels,\n",
    ")\n",
    "from src.text_encoder import TextEncoder\n",
    "from src.structured_encoder import StructuredEncoder\n",
    "from src.fusion_model import DeepTriageCN\n",
    "from src.tabnet_model import TabNetWrapper\n",
    "from src.baseline_models import (\n",
    "    VitalsOnlyXGBoost,\n",
    "    RandomForestBaseline,\n",
    "    TextOnlyLogisticRegression,\n",
    ")\n",
    "from src.clinical_scores import compute_news2, compute_mews, compute_esi_level\n",
    "\n",
    "logging.basicConfig(level=logging.INFO)\n",
    "logger = logging.getLogger(__name__)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Load configuration\n",
    "with open(\"../config.yaml\", \"r\") as f:\n",
    "    config = yaml.safe_load(f)\n",
    "\n",
    "data_cfg = config[\"data\"]\n",
    "xgb_cfg = config[\"xgboost\"]\n",
    "tabnet_cfg = config[\"tabnet\"]\n",
    "output_dir = Path(config[\"output_paths\"][\"models_dir\"])\n",
    "output_dir.mkdir(parents=True, exist_ok=True)\n",
    "random_state = config[\"random_state\"]"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Load and split data\n",
    "train_df, val_df = load_and_split_data(\n",
    "    data_cfg[\"raw_data_path\"],\n",
    "    training_cutoff_date=data_cfg[\"training_cutoff_date\"],\n",
    "    validation_start_date=data_cfg[\"validation_start_date\"],\n",
    "    random_state=random_state,\n",
    ")\n",
    "X_train_raw, X_val_raw, scaler = preprocess_structured(train_df, val_df)\n",
    "train_texts, val_texts = preprocess_text(train_df, val_df)\n",
    "y_train, y_val = get_labels(train_df, val_df)\n",
    "\n",
    "print(f\"X_train shape: {X_train_raw.shape}\")\n",
    "print(f\"X_val shape: {X_val_raw.shape}\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Calculate scale_pos_weight\n",
    "n_neg = (y_train == 0).sum()\n",
    "n_pos = (y_train == 1).sum()\n",
    "scale_pos_weight = n_neg / n_pos if n_pos > 0 else 1.0\n",
    "print(f\"Scale pos weight: {scale_pos_weight:.2f}\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# 1. DeepTriage-CN (multimodal)\n",
    "text_encoder = TextEncoder(model_name=config[\"text_processing\"][\"model_name\"],\n",
    "                           max_length=config[\"text_processing\"][\"max_length\"])\n",
    "deeptriage = DeepTriageCN(text_encoder=text_encoder, random_state=random_state)\n",
    "deeptriage.fit(train_texts.tolist(), X_train_raw, y_train, scale_pos_weight=scale_pos_weight)\n",
    "deeptriage.save(str(output_dir / \"deeptriage_cn.pkl\"))\n",
    "print(\"DeepTriage-CN trained and saved.\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# 2. TabNet (structured only)\n",
    "tabnet = TabNetWrapper(\n",
    "    n_d=tabnet_cfg[\"n_d\"],\n",
    "    n_a=tabnet_cfg[\"n_a\"],\n",
    "    n_steps=tabnet_cfg[\"n_steps\"],\n",
    "    gamma=tabnet_cfg[\"gamma\"],\n",
    "    lambda_sparse=tabnet_cfg[\"lambda_sparse\"],\n",
    "    seed=random_state,\n",
    ")\n",
    "# TabNet requires eval set for early stopping\n",
    "eval_set = [(X_val_raw, y_val)]\n",
    "tabnet.fit(X_train_raw, y_train, eval_set=eval_set,\n",
    "           batch_size=tabnet_cfg[\"batch_size\"],\n",
    "           virtual_batch_size=tabnet_cfg[\"virtual_batch_size\"],\n",
    "           max_epochs=tabnet_cfg[\"max_epochs\"])\n",
    "tabnet.save(str(output_dir / \"tabnet\"))  # saves .zip + .wrapper\n",
    "print(\"TabNet trained and saved.\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# 3. Vitals-only XGBoost\n",
    "vit_xgb = VitalsOnlyXGBoost(\n",
    "    n_estimators=xgb_cfg[\"n_estimators\"],\n",
    "    learning_rate=xgb_cfg[\"learning_rate\"],\n",
    "    max_depth=xgb_cfg[\"max_depth\"],\n",
    "    subsample=xgb_cfg[\"subsample\"],\n",
    "    colsample_bytree=xgb_cfg[\"colsample_bytree\"],\n",
    "    random_state=random_state,\n",
    ")\n",
    "vit_xgb.fit(X_train_raw, y_train)\n",
    "joblib.dump(vit_xgb, output_dir / \"vitals_only_xgb.pkl\")\n",
    "print(\"Vitals-only XGBoost trained.\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# 4. Random Forest\n",
    "rf = RandomForestBaseline(n_estimators=200, max_depth=6, random_state=random_state)\n",
    "rf.fit(X_train_raw, y_train)\n",
    "joblib.dump(rf, output_dir / \"random_forest.pkl\")\n",
    "print(\"Random Forest trained.\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# 5. Text-only Logistic Regression\n",
    "# We'll extract text embeddings here\n",
    "train_emb = text_encoder.encode(train_texts.tolist())\n",
    "text_lr = TextOnlyLogisticRegression(random_state=random_state)\n",
    "text_lr.fit(train_emb, y_train)\n",
    "joblib.dump(text_lr, output_dir / \"text_only_lr.pkl\")\n",
    "print(\"Text-only LR trained.\")"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# 6. Clinical scores (computed on validation set for benchmarking)\n",
    "val_scores = pd.DataFrame({\n",
    "    \"NEWS2\": compute_news2(\n",
    "        val_df[\"respiratory_rate\"].values,\n",
    "        val_df[\"spo2\"].values,\n",
    "        val_df[\"temperature\"].values,\n",
    "        val_df[\"sbp\"].values,\n",
    "        val_df[\"heart_rate\"].values,\n",
    "        \n",
    "    ),\n",
    "    \"MEWS\": compute_mews(\n",
    "        val_df[\"respiratory_rate\"].values,\n",
    "        val_df[\"heart_rate\"].values,\n",
    "        val_df[\"sbp\"].values,\n",
    "        val_df[\"temperature\"].values,\n",
    "    ),\n",
    "    \"ESI\": compute_esi_level(\n",
    "        val_df[\"age\"].values,\n",
    "        val_df[\"heart_rate\"].values,\n",
    "        val_df[\"respiratory_rate\"].values,\n",
    "        val_df[\"sbp\"].values,\n",
    "        val_df[\"spo2\"].values,\n",
    "        val_df[\"temperature\"].values,\n",
    "    ),\n",
    "})\n",
    "val_scores.to_csv(output_dir / \"clinical_scores_val.csv\", index=False)\n",
    "print(\"Clinical scores computed and saved.\")"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "All models have been trained and saved to `outputs/models/`. The next notebook will evaluate their performance on the validation set."
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "deeptriage-cn",
   "language": "python",
   "name": "deeptriage-cn"
  },
  "language_info": {
   "name": "python",
   "version": "3.9"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}